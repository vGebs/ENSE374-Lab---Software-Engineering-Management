<% 
if(tasks.length > 0){
    for(const task of tasks) {
        if (task.isTaskDone == true && task.claimingUser == username && task.isTaskCleared == false){ 
%>
<form action="/unFinishTask" method="post">
    <div class="input-group mb-3">

        <input type='hidden' name='taskID' value='<%=task.id%>'>
        <input type='submit' class="btn btn-outline-secondary" id="button-addon2" value='Unfinish'>

        <input type="text" class="form-control" aria-label="Text input with checkbox"
            placeholder="<%= task.text %> completed by me" aria-label="Disabled input example" disabled readonly>
    </div>
</form>


<%  } } } %>