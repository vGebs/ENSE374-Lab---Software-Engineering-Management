<% 
if(tasks.length > 0){
    for(const task of tasks) {
        if (task.isTaskDone == false && task.claimingUser != 0 && task.isTaskClaimed == true){ 
%>
<input class="form-control" type="text"
    value="<%=task.text %> claimed by another user"
    aria-label="Disabled input example" disabled readonly>
<br>

<%  } } } %>