<% 
if(tasks.length > 0){
    for(const task of tasks) {
        if (task.isTaskDone == true && task.claimingUser != username){ 
%>
<input class="form-control completed" type="text" value="<%= task.text %>"
    aria-label="Disabled input example" disabled readonly>
<br>

<%  } } } %>