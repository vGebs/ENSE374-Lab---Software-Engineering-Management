<% 
if(tasks.length > 0){
    for(const task of tasks) {
        if (task.isTaskClaimed == false){ 
%>
<form action='/claimTask' method="post">
    <div class="claimTask">
        <div class="input-group mb-3">
            <input type="text" class="form-control" name="claimed" value="<%= task.text %>"
                aria-label="Recipient's username" aria-describedby="button-addon2" disabled readonly>
            <input type="hidden" name="taskID" value="<%=task.id%>">
            <input type='submit' class="btn btn-outline-secondary" id="button-addon2" value="Claim">
        </div>
    </div>
</form>

<%  } } } %>
